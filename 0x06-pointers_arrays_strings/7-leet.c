#include "main.h"
/**
 * leet - function
 * @str: string
 * Return: char
 */
char *leet(char *s)
{
	int i = 0, k;
	char s[] = "aAeEotTLl";
	char S[] = "4433007711";

	for (; str[i] != '\0'; i++)
	{
		for (k = 0; k <= 9; k++)
		{
			str[i] = S[k];
		}
	}
	return (str);
}
